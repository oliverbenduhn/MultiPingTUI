# Ideen

ich hätte gerne einen reinen Webservermodus, also einen modus, wo in der konsole nix angezeigt wird, sondern nur die live webansicht läuft. z.b. mping -webserver

Konfigurationsdatei für Standard-Settings (~/.config/mping/config.yaml)
Speichern  der Daten beim verlassen.
öffnen der Standard-Setting beim Starten.

Historische Daten persistent speichern (SQLite/TimescaleDB)

Gruppieren nach Subnetzen

Auto-Discovery via ARP/mDNS/LLDP
